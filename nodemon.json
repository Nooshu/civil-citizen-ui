{
  "watch": ["src/main"],
  "ext": "ts,js,njk,css",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "./bin/generate-ssl-options.sh && ./node_modules/.bin/ts-node -r tsconfig-paths/register ./src/main/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
