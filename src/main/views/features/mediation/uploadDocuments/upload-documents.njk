{% extends "claim-details-tpl.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "../../../macro/error-summary.njk" import errorSummary %}
{% from "../../../macro/csrf.njk" import csrfProtection %}
{% from "../../../macro/contact-us-for-help.njk" import contactUsForHelp with context %}
{% from "../../../macro/page-title.njk" import setPageTitle %}
{% from "../../dashboard/claim-summary-content.njk" import claimSummaryContent %}
{% from "../../../macro/button.njk" import addButton %}

{% block pageTitle %}
  {{ setPageTitle(t, pageTitle) }}
{% endblock %}
{% block content %}
<div class="govuk-width-container caseProgression">

    {% if form != null and form.hasErrors() %}
      {{ errorSummary(form.getAllErrors(), t) }}
    {% endif %}

    <div class="govuk-grid-row">
      <form method="post" action="{{ currentUrl }}?_csrf={{ csrf }}" enctype='multipart/form-data' novalidate>
        {{ csrfProtection(csrf) }}
        <div id="content"></div>

        <div class="govuk-grid-column-two-thirds govuk-!-margin-top-0">
          <h1 class="govuk-heading-l">{{ t(pageTitle) }}</h1>

          <h2 class="govuk-body-l">{{t('COMMON.CASE_REFERENCE', {claimId: claimId})}}</h2>

          <h2 class="govuk-body-l">{{partyInformation.claimantName}} v {{partyInformation.defendantName}}</h2>

          <h2 class="govuk-heading-s">{{ t(subtitle) }}</h2>
          <p class="govuk-body-m">{{ t(paragraph) }}</p>
          <h1 class="govuk-heading-l">{{ t(sectionTitle) }}</h1>
         <h2 class="govuk-visually-hidden" id="your-statement-heading">
          {{ t('PAGES.UPLOAD_DOCUMENTS.STATEMENT') }}
         </h2>

            {% if yourStatementContent.length > 0 %}

            {% for yourStatementSection in yourStatementContent %}
            <div class="row-container" role="group" aria-labelledby="your-statement-heading">
              <div data-module="cui-add-another">
                {% for section in yourStatementSection %}

                <span class="govuk-visually-hidden">
                  {{ section.title or t(pageTitle) }}
                </span>

                {# Div, not govukFieldset: an empty legend plus inner GOV.UK date fieldsets nested
                   fieldsets. Add another is a server POST here and must no-op without `.cui-add-another__items`. #}
                <div class="cui-add-another__item">
                  {{ claimSummaryContent({ contentSections: section.contentSections }, t) }}
                </div>
                {% endfor %}
                <div class="govuk-button-group">
                  {{ govukButton({text: t('PAGES.UPLOAD_DOCUMENTS.ADD'),
                    classes: 'govuk-button--secondary govuk-!-margin-bottom-1',
                    name: 'action',
                    value: 'add_another-yourStatement',
                    attributes: {
                    'id': 'add-another-yourStatement'
                  }
                  }) }}
                </div>
                <hr class="govuk-section-break--l govuk-section-break--visible">
              </div>
            </div>
            {% endfor %}
            {% endif %}
         <h2 class="govuk-visually-hidden" id="documents-referred-heading">Documents referred section</h2>
          {% if documentsReferredContent.length > 0 %}
          {% for documentsReferredSection in documentsReferredContent %}
          <div class="row-container" role="group" aria-labelledby="documents-referred-heading">
            <div data-module="cui-add-another">

              {% for section in documentsReferredSection %}

              <span class="govuk-visually-hidden">
                {{ section.title or t(pageTitle) }}
              </span>

              <div class="cui-add-another__item">
                {{ claimSummaryContent({contentSections: section.contentSections }, t) }}
              </div>
              {% endfor %}
              <div class="govuk-button-group">
                {{ govukButton({ text: t('PAGES.UPLOAD_DOCUMENTS.ADD'),
                  classes: 'govuk-button--secondary govuk-!-margin-bottom-1',
                  name: 'action',
                  value: 'add_another-documentsReferred',
                  attributes: {
                  'id': 'add_another-documentsReferred'
                }
                }) }}
              </div>
              <hr class="govuk-section-break--l govuk-section-break--visible">
            </div>
          </div>
          {% endfor %}
          {% endif %}
              {{ addButton(t, 'CONTINUE',cancelUrl) }}
              {{ contactUsForHelp(t) }}
        </div>
      </form>
    </div>

</div>

<script nonce="{{ nonceValue }}">
(() => {
  const summary = document.querySelector('.govuk-error-summary');
  if (!summary) return;

  summary.addEventListener('click', e => {
    const link = e.target.closest('a[href^="#"]');
    if (!link) return;

    const el = document.getElementById(link.hash.slice(1));
    if (!el) return;

    setTimeout(() => {
      el.scrollIntoView({ block: 'start' });
      window.scrollBy(0, -160);
    });
  });
})();
</script>

{% endblock %}
