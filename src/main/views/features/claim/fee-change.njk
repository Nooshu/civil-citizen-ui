{% extends "claim-details-tpl.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "../../macro/contact-us-for-help.njk" import contactUsForHelp with context %}
{% from "../../macro/page-title.njk" import setPageTitle %}

{% block pageTitle %}
  {{ setPageTitle(t, pageTitle) }}
{% endblock %}

{% block content %}

      <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
          <h1 class="govuk-heading-l">{{ t("PAGES.FEE_CHANGE.TITLE") }}</h1>
          <p class="govuk-body">{{ t("PAGES.FEE_CHANGE.PARAGRAPH_1") }}</p>
          <p class="govuk-body">{{ t("PAGES.FEE_CHANGE.PARAGRAPH_2", { claimFee : claimFee }) }}</p>
          <p class="govuk-body">{{ t("PAGES.FEE_CHANGE.PARAGRAPH_3") }}</p>
          {# Link-button (href), not a POST — wrapping it in a form made HTML_CodeSniffer
             report a missing submit control (H32.2). Official govukButton-as-link is the control. #}
          <div class="govuk-form-group">
            {{ govukButton({
              text: t('PAGES.FEE_CHANGE.BUTTON_LABEL'),
              href: redirectUrl
            }) }}
          </div>
        </div>
      </div>
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-three-thirds">
          {{ contactUsForHelp(t) }}
        </div>
      </div>

{% endblock %}
