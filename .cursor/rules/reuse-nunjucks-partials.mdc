---
description: Reuse Nunjucks partials and GOV.UK macros; no duplicated shared markup
alwaysApply: true
---

# Reuse Nunjucks partials and GOV.UK macros

Do not copy-paste UI markup. Compose pages from shared partials and official macros.

- Prefer **GOV.UK Frontend Nunjucks macros** for Design System components (buttons, inputs, error summaries, panels, etc.) — see `.cursor/rules/govuk-frontend-ui.mdc`
- Prefer **shared Nunjucks partials / app macros** under `src/main/views/` (for example `layout.njk`, `macro/`, feature shared templates) over duplicating the same HTML across pages
- When the same block appears on more than one page, extract a partial or macro and `{% include %}` / call it — do not maintain parallel copies
- Shared journey chrome (headers, claim details panels, confirmation content fragments) especially: reuse existing templates and macros; do not reimplement markup per page
- New pages extend the established layout (`layout.njk` / feature layouts) and import shared chrome rather than inlining it
- If a shared partial is missing, add one in the right `views/` folder and switch callers to it in the same change
- Update comments and docs when introducing a new reusable partial so others know to use it
